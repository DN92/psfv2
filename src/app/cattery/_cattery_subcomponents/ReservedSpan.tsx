import styles from '../kittens/kittens.module.css';

const ReservedSpan: React.FC = () => {

  return (
    <span className={ styles.reserved_span }>RESERVED</span>
  );
};

export default ReservedSpan;
